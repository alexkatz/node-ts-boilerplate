import { luna, tycho } from './cats';

const thing = 5;

const fn = (foo: number, bar: string) => {
  console.log(bar);
};

console.log(luna);
console.log(luna.greeting());
console.log(tycho);
console.log(tycho.greeting());
